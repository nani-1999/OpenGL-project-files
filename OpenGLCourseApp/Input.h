#pragma once

#include <GLFW/glfw3.h>

class Input {
public:
	Input();
	~Input();

//	void BindKeyInputs(GLFWwindow* window);
//
//private:
//	bool bKeyStates[1024] = { false };
//
//	static void InputEvent_Callback(GLFWwindow* window, int key, int code, int action, int mode); /* must be static */
};

